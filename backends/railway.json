{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "ls -la && apt-get update && apt-get install -y maven && mvn clean package -DskipTests"
  },
  "deploy": {
    "startCommand": "java -jar target/*.jar",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}