{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "ls -la && echo 'Files listed successfully'"
  },
  "deploy": {
    "startCommand": "java -jar target/*.jar",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}